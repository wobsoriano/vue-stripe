---
"vue-stripe": minor
---

Add `LinkSignupElement` to `vue-stripe` and `vue-stripe/checkout`. It requires Stripe beta access, and the checkout version works only inside `CheckoutElementsProvider`.

Raise the `@stripe/stripe-js` peer range to `>=9.16.0 <10.0.0`.
